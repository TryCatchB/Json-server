{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Json-server",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "server",
    "json"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "json-server": "^1.0.0-beta.3"
  }
}
